Hey — I started a little text-analysis game but it's half-broken and I ran out of time. There's a Flask API in /app/api/app.py (port 5000) with /analyze, /evaluate, and /compare, plus a CLI in /app/game.py that loads a document, shows its stats, takes a typed summary, scores it via the API, and prints results. A large document is already sitting in /app/documents/ (don't shrink it). Please get the whole thing working and behaving the way I describe below — right now the numbers are wrong, the error handling is sloppy, and the game won't run non-interactively. flask and requests are installed; keep them listed in /app/requirements.txt.

/analyze takes {"text"} and returns word_count, unique_word_count, sentence_count, key_terms (a list), and estimated_tokens. A couple of things the current version gets wrong: word_count should count only real words — contractions and hyphenated words like "don't" and "well-known" each count once, but digits and punctuation don't count. unique_word_count is the distinct words, case-insensitive (so "The" and "the" are the same word). sentence_count must not be fooled by decimals or title abbreviations — "Dr. Smith measured 3.5 degrees today." is one sentence, not several. A final sentence that runs to the end with no closing punctuation should still count, and a run of terminators like "..." or "?!" counts as a single boundary, not several. And key_terms should be the meaningful topic words, with common stopwords (the, and, there, would, about, and the like) filtered out, ordered from most to least frequent.

/evaluate takes {"original","summary"} and returns a 0–100 score plus a feedback string that actually varies with the result; weight topic/key-term coverage well above length, so an on-topic summary clearly beats an equally long off-topic one. /compare takes {"original","summary_a","summary_b"} and returns winner ("a", "b", or "tie"), score_a, score_b, and margin (the non-negative absolute difference). It must be symmetric — swapping the summaries swaps the scores and keeps the same margin — and ties are decided by score, so two summaries that score the same should tie even if their text differs.

All three endpoints must return a 400 with a JSON body (not Flask's HTML error page) when any required field is missing or only whitespace. Make /app/game.py executable; the summary must be the only thing it reads from stdin — no menus or mode prompts — so `echo "..." | python /app/game.py doc.txt` works, and it should show the sentence count among the stats. Run Flask with debug=False, and don't leave a server running on port 5000 when you're done so the tests can bind it.

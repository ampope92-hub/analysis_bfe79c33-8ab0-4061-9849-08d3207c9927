Hey, I'm building a little text-analysis game and need it wired together. There's a Flask API on port 5000 at /app/api/app.py with three POST endpoints, plus a CLI script /app/game.py that loads a document, shows its stats, scores a typed summary via the API, and prints results. Put a .txt file of at least 50,000 tokens — roughly 37,500+ whitespace-delimited words — in /app/documents/ (no tiny files), and list flask and requests in /app/requirements.txt.

/analyze takes {"text"} and returns word_count (alphabetic tokens only — skip digits, punctuation, and mixed strings like "abc123"), unique_word_count (distinct alphabetic words, case-insensitive), sentence_count, key_terms (a list), and estimated_tokens. /evaluate takes {"original","summary"} and returns a 0–100 score plus a feedback string reflecting the result; weight topic/key-term coverage well above length, so an on-topic summary clearly beats an equally long off-topic one. /compare takes {"original","summary_a","summary_b"} and returns winner (exactly "a", "b", or "tie"), score_a, score_b, and margin (the non-negative absolute difference); it must be symmetric (swapping the summaries swaps the scores, same margin) and identical summaries tie.

All three endpoints must return a 400 with a JSON body (not Flask's HTML abort page) when any required field is missing or only whitespace. Make /app/game.py executable; the summary must be the only thing it reads from stdin (no menus or extra prompts) so `echo "..." | python /app/game.py doc.txt` works, and show the sentence count among the stats. Run Flask with debug=False (avoids reloader background-timeout issues), and shut down any Flask process on port 5000 when you're done so the test can bind it.
